/* Operadores aritmeticos
= Operador de asignacion. Se usa para asignar valores a una variable
+ Suma
- Resta
* Multiplicacion
/ Division
% Modulo
++ Aumento
-- Disminucion
*/

//Suma
const resultado = 10+10;
const resultado1 = 10-10;
const resultado2 = 10 % 3

let numero = 1;
numero ++ ;
console.log(numero);

/* operadores de asignacion
    += suma un numero al valor de una variable
    -= resta un numero al valor de una variable
    *= resta un numero al valor de una variable
    /= resta un numero al valor de una variable
    %= obtiene el sobrante de una divicion y lo asigna a la variable
*/

let num = 10;
num += 5;
console.log(num);

/*  OPERADORES DE COMPARACION
    == Igual que
    === Igual en valor y tipo de valor
    != Diferente
    !== Diferente en valor y diferente en tipo
    > mayor
    < menor
    >= mayor o igual que
    <= menor o igual
    ? operador ternario

*/

const result = 5 > 1;
const resulta = 10 == 10;
const res = 10 === '10';
console.log(res);

// operador ternario
const resu = 7 > 2 ? 'El primer numero es mayor que el segundo': 'El segundo valor es mayor que el primero'
console.log(resu);